# find_aces_root.py

Purpose
- Scan ACE names and `ace_raw` blobs to find occurrences of the substring "root".

Usage
- `python scripts/find_aces_root.py`

Outputs
- Prints a ranking of matching ACE names and counts to stdout.
