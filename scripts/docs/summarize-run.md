# summarize-run.ps1

Purpose: Produce a small CSV summarizing counts across a run folder (appliances, volumes, shares, folder ACL count estimates).

Usage:

```powershell
./scripts/summarize-run.ps1 -RunPath <run-folder> -OutPath results/run-summary.csv
```
